// background.js

// Function to handle onInstalled event
chrome.runtime.onInstalled.addListener(function(details) {
  console.log('Extension installed or updated!');

  // Perform any setup tasks here
});
